{
  "_comment": "Copy to appsettings.Development.json (gitignored) or use `dotnet user-secrets` instead. Never commit real secrets here.",
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=RepairRequestDb;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True"
  },
  "Cors": {
    "AllowedOrigins": [ "http://localhost:4200" ]
  },
  "DevelopmentMalwareScanning": {
    "_comment": "Development/Testing-only FAKE scanner (DEC-PRE-S1-007-08) so uploads can become CLEAN locally. It provides no malware protection and the API refuses to start with it enabled in any other environment.",
    "Enabled": true
  }
}
